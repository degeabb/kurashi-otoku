/**
 * Language switcher — simplified for multi-URL architecture.
 * Each language has its own page, so no auto-detection or content swapping needed.
 * The active button is set in HTML. This JS only handles click navigation.
 */
